#include "main.h"

/**
 * mul - multiply its two parameter.
 * @a: the first paremeter if type int
 * @b: the second parameter of type int
 * Return: the result of multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
