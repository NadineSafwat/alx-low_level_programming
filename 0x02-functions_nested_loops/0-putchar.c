#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always returrns zero when sucess
 *
 */

int main(void)
{
	char ud = '_';
	char p = 'p';
	char u = 'u';
	char t = 't';
	char c = 'c';
	char h = 'h';
	char a = 'a';
	char r = 'r';
	char nl = '\n';

	putchar(ud);
	putchar(p);
	putchar(u);
	putchar(t);
	putchar(c);
	putchar(h);
	putchar(a);
	putchar(r);
	putchar(nl);
	return (0);
}
